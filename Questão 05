def num(a, b, c):
    if a < b < c:
        return a, b, c
    if a < c < b:
        return a, c, b
    if b < a < c:
        return b, a, c
    if b < c < a:
        return b, c, a
    if c < a < b:
        return c, a, b
    if c < b < a:
        return c, b, a
    if a == b or a == c or b == c:
        return

def main():
    n1 = int(input())
    n2 = int(input())
    n3 = int(input())

    resultado = num(n1, n2, n3)

    print(resultado)


if __name__
    main()
